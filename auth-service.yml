# For service discovery
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: '*'

application:
  jwt:
    expiration: P1D # 1 day